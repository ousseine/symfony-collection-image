<form name="delete_form" action="{{ path('wedding_delete', {id: wedd.id}) }}" method="post">
    <input type="hidden" name="_token" value="{{ csrf_token('delete') }}" />

    <button type="submit" class="btn-danger">Supprimer</button>
</form>